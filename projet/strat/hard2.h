#include "../grid.h"
#include "strategy.h"
#include <stdlib.h>
#include <stdio.h>
#include <math.h>
#include <stdbool.h>


int evaluation2(grid g);
enum dir best_ev(strategy s, grid g);

strategy hard2(grid g);
